<div class="container">
  <% @flats.each do |flat| %>
    <%= flat.name %>
    <%= flat.address %>
    <%= flat.description %>
    <%= flat.price_per_night %>
    <%= flat.number_of_guests  %>
  <% end %>
</div>
